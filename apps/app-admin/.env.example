# Control database (same as super-admin)
CONTROL_DB_HOST=localhost
CONTROL_DB_PORT=5432
CONTROL_DB_NAME=Shribi Edufy_control
CONTROL_DB_USER=postgres
CONTROL_DB_PASSWORD=

# Auth (must match super-admin JWT secret for shared tokens, or use a dedicated secret)
JWT_SECRET=your-secret-key

# Optional
NEXT_PUBLIC_APP_URL=http://localhost:7006
